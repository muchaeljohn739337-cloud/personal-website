# Advancia Pay - Frontend Deployment Architecture

## 🏗️ Complete Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           INTERNET USERS                             │
│                     (Browsers, Mobile Apps)                          │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      CLOUDFLARE CDN (Global)                         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  DNS Resolution & SSL/TLS Termination                       │   │
│  │  • advancia.app          → Frontend (Proxied)               │   │
│  │  • www.advancia.app      → Frontend (Proxied)               │   │
│  │  • admin.advancia.app    → Frontend (Proxied)               │   │
│  │  • api.advancia.app      → Backend (via Worker)             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Security Layer (WAF)                                       │   │
│  │  • DDoS Protection                                          │   │
│  │  • Rate Limiting (100 req/min)                              │   │
│  │  • Bot Detection                                            │   │
│  │  • SQL Injection/XSS Blocking                               │   │
│  │  • Geo-blocking (optional)                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Cloudflare Worker (API Gateway)                            │   │
│  │  • CORS Handling                                            │   │
│  │  • Security Headers Injection                               │   │
│  │  • Request Logging                                          │   │
│  │  • Backend Proxy (api.advancia.app → Render)               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Caching Layer                                              │   │
│  │  • Static Assets: 1 month cache                             │   │
│  │  • HTML Pages: 2 hour cache                                 │   │
│  │  • API Requests: No cache (bypass)                          │   │
│  │  • Brotli/Gzip Compression                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└────────────┬────────────────────────────────┬─────────────────────┘
             │                                 │
             ▼                                 ▼
┌─────────────────────────────┐  ┌────────────────────────────────┐
│   RENDER - FRONTEND         │  │   RENDER - BACKEND             │
│   (Next.js 14)              │  │   (Node.js + Express)          │
│                             │  │                                │
│  Service: advancia-frontend │  │  Service: advancia-backend     │
│  Region: Oregon (US West)   │  │  Region: Oregon (US West)      │
│  Plan: Free / Starter       │  │  Plan: Free / Starter          │
│                             │  │                                │
│  ┌───────────────────────┐ │  │  ┌──────────────────────────┐ │
│  │  Next.js Application  │ │  │  │  Express API Server       │ │
│  │  • App Router         │ │  │  │  • REST API               │ │
│  │  • Server Components  │ │  │  │  • WebSocket (Socket.IO)  │ │
│  │  • Static Generation  │ │  │  │  • JWT Auth               │ │
│  │  • Image Optimization │ │  │  │  • Prisma ORM             │ │
│  └───────────────────────┘ │  │  └──────────────────────────┘ │
│                             │  │                                │
│  Build: npm run build       │  │  Build: npm run build          │
│  Start: npm start           │  │  Start: npm run dev            │
│  Port: 3000 (internal)      │  │  Port: 4000 (internal)         │
│                             │  │                                │
│  Environment:               │  │  Environment:                  │
│  • NODE_ENV=production      │  │  • NODE_ENV=production         │
│  • NEXT_PUBLIC_API_URL      │  │  • DATABASE_URL                │
│    =https://api.advancia.app│  │  • JWT_SECRET                  │
│                             │  │  • ALLOWED_ORIGINS             │
└──────────────┬──────────────┘  └────────────┬───────────────────┘
               │                              │
               │                              ▼
               │                 ┌────────────────────────────────┐
               │                 │  PostgreSQL Database           │
               │                 │  (Render Managed DB)           │
               │                 │                                │
               │                 │  • User accounts               │
               │                 │  • Transactions                │
               │                 │  • Wallets                     │
               │                 │  • Doctor records              │
               │                 │  • Consultations               │
               │                 │  • Audit logs                  │
               └─────────────────┤                                │
                    API Calls    │  Host: dpg-*.postgres.render   │
                                 │  Port: 5432                    │
                                 │  SSL: Required                 │
                                 └────────────────────────────────┘
```

---

## 🔄 Request Flow Diagrams

### Frontend Page Request Flow

```
User types: https://advancia.app
    │
    ▼
Cloudflare DNS resolves to Cloudflare edge
    │
    ▼
Cloudflare checks cache
    │
    ├─ Cache HIT → Return cached page (fast!)
    │
    └─ Cache MISS ─┐
                   │
                   ▼
           Cloudflare forwards to Render
                   │
                   ▼
           Render serves Next.js page
                   │
                   ▼
           HTML returned to Cloudflare
                   │
                   ▼
           Cloudflare caches for 2 hours
                   │
                   ▼
           HTML returned to user
                   │
                   ▼
           Browser renders page
                   │
                   ▼
           Browser requests static assets
                   │
                   ▼
           Cloudflare serves from cache (30 days)
```

### API Request Flow

```
Frontend makes API call: fetch('https://api.advancia.app/api/health')
    │
    ▼
Cloudflare DNS resolves api.advancia.app
    │
    ▼
Cloudflare Worker intercepts request
    │
    ├─ Check CORS origin
    ├─ Check rate limit (100/min)
    ├─ Add security headers
    └─ Log request
    │
    ▼
Worker proxies to: advancia-backend.onrender.com
    │
    ▼
Backend Express server handles request
    │
    ├─ Authentication middleware
    ├─ Route handler
    └─ Database query (if needed)
    │
    ▼
Response returned to Worker
    │
    ▼
Worker adds security headers
    │
    ▼
Response returned to frontend
    │
    ▼
Frontend updates UI
```

### WebSocket Connection Flow

```
Frontend connects: io('https://api.advancia.app')
    │
    ▼
Cloudflare receives WebSocket upgrade request
    │
    ▼
Cloudflare proxies to Backend (WebSocket support)
    │
    ▼
Backend Socket.IO accepts connection
    │
    ▼
Persistent bidirectional connection established
    │
    ├─ Server can push notifications
    ├─ Client can send messages
    └─ Real-time updates for transactions, etc.
```

---

## 🏢 Multi-Region Architecture (Future)

```
┌──────────────────────────────────────────────────────────────────┐
│                    CLOUDFLARE CDN (Global)                       │
│                  Edge locations in 200+ cities                   │
└────────┬────────────────────┬─────────────────┬─────────────────┘
         │                    │                 │
    ┌────┴──────┐    ┌────────┴────────┐   ┌───┴──────────┐
    │ US West   │    │    EU West      │   │  Asia Pacific │
    │  (Oregon) │    │   (Frankfurt)   │   │  (Singapore)  │
    └────┬──────┘    └────────┬────────┘   └───┬──────────┘
         │                    │                 │
    ┌────▼──────┐    ┌────────▼────────┐   ┌───▼──────────┐
    │ Frontend  │    │   Frontend      │   │   Frontend   │
    │ Backend   │    │   Backend       │   │   Backend    │
    │ Database  │    │   Read Replica  │   │   Read Rep.  │
    └───────────┘    └─────────────────┘   └──────────────┘
         │                    │                 │
         └────────────────────┴─────────────────┘
                              │
                    ┌─────────▼──────────┐
                    │  Primary Database  │
                    │    (US West)       │
                    └────────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 7: Application Security                              │
│ • JWT Authentication                                        │
│ • Role-Based Access Control (RBAC)                          │
│ • Input Validation (Zod schemas)                            │
│ • SQL Injection Prevention (Prisma ORM)                     │
│ • XSS Prevention (React escaping)                           │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 6: Cloudflare Worker                                 │
│ • CORS Policy Enforcement                                   │
│ • Rate Limiting (per IP)                                    │
│ • Request/Response Logging                                  │
│ • Security Headers Injection                                │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 5: Cloudflare WAF                                     │
│ • OWASP Top 10 Protection                                   │
│ • Custom Firewall Rules                                     │
│ • Bot Detection & Mitigation                                │
│ • Geo-blocking                                              │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 4: DDoS Protection                                    │
│ • Network Layer DDoS (L3/L4)                                │
│ • Application Layer DDoS (L7)                               │
│ • Always-on Protection                                      │
│ • Unmetered Mitigation                                      │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: SSL/TLS Encryption                                 │
│ • TLS 1.3 (with fallback to 1.2)                            │
│ • Full (strict) encryption mode                             │
│ • HSTS Preload                                              │
│ • Certificate pinning                                       │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: DNS Security                                       │
│ • DNSSEC                                                    │
│ • Proxied DNS (hide origin)                                 │
│ • DNS rate limiting                                         │
└─────────────────────────────────────────────────────────────┘
                           ▲
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: Network Security                                   │
│ • Origin IP hidden                                          │
│ • Render private networking                                 │
│ • Database SSL/TLS required                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       │ 1. User registers/logs in
       ▼
┌─────────────────────┐
│  Frontend (React)   │
│  • Forms            │
│  • Validation       │
│  • State management │
└──────┬──────────────┘
       │
       │ 2. POST /api/auth/login
       ▼
┌────────────────────────┐
│  Cloudflare Worker     │
│  • CORS check          │
│  • Rate limit check    │
│  • Forward to backend  │
└──────┬─────────────────┘
       │
       │ 3. Proxied request
       ▼
┌──────────────────────────────┐
│  Backend (Express)           │
│  • authenticateToken()       │
│  • bcrypt.compare()          │
│  • jwt.sign()                │
└──────┬───────────────────────┘
       │
       │ 4. Query user
       ▼
┌────────────────────────┐
│  Database (Prisma)     │
│  • findUnique()        │
│  • User table          │
└──────┬─────────────────┘
       │
       │ 5. User data
       ▼
┌──────────────────────────────┐
│  Backend                     │
│  • Generate JWT              │
│  • Return {token, user}      │
└──────┬───────────────────────┘
       │
       │ 6. Response
       ▼
┌────────────────────────┐
│  Cloudflare Worker     │
│  • Add security headers│
│  • Log response        │
└──────┬─────────────────┘
       │
       │ 7. Response with headers
       ▼
┌─────────────────────┐
│  Frontend           │
│  • Store token      │
│  • Redirect to /dashboard
└──────┬──────────────┘
       │
       │ 8. Navigate
       ▼
┌─────────────────────┐
│  Dashboard Page     │
│  • Fetch user data  │
│  • Display UI       │
└─────────────────────┘
```

---

## 🚀 Deployment Pipeline

```
┌──────────────────────────────────────────────────────────────────┐
│                      DEVELOPER WORKFLOW                          │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                     ┌──────┴───────┐
                     │ Local Dev    │
                     │ (Windows)    │
                     │ • npm run dev│
                     │ • Testing    │
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │ Git Commit   │
                     │ git push     │
                     └──────┬───────┘
                            │
┌───────────────────────────▼────────────────────────────────────┐
│                    GITHUB REPOSITORY                            │
│  • Source code                                                  │
│  • Version control                                              │
│  • Branch protection                                            │
└───────────────────────────┬────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              │                           │
     ┌────────▼────────┐        ┌────────▼────────┐
     │ GitHub Actions  │        │ Render Auto     │
     │ CI/CD Workflow  │        │ Deploy          │
     │                 │        │                 │
     │ • Lint          │        │ • Detect push   │
     │ • Build         │        │ • Clone repo    │
     │ • Test          │        │ • Install deps  │
     │ • Deploy        │        │ • Build         │
     └────────┬────────┘        └────────┬────────┘
              │                           │
              └─────────────┬─────────────┘
                            │
              ┌─────────────▼─────────────┐
              │   Render Build Server     │
              │   • npm ci                │
              │   • npm run build         │
              │   • Create container      │
              └─────────────┬─────────────┘
                            │
              ┌─────────────▼─────────────┐
              │   Render Deploy           │
              │   • Health check          │
              │   • Zero-downtime         │
              │   • Start new instance    │
              │   • Stop old instance     │
              └─────────────┬─────────────┘
                            │
              ┌─────────────▼─────────────┐
              │   Production Live         │
              │   advancia.app            │
              │   (via Cloudflare)        │
              └───────────────────────────┘
```

---

## 📈 Monitoring & Observability

```
┌────────────────────────────────────────────────────────────────┐
│                   MONITORING STACK                             │
└────────────────────────────────────────────────────────────────┘

┌─────────────────────┐  ┌─────────────────────┐  ┌──────────────┐
│ Cloudflare          │  │ Render Dashboard    │  │ Prisma       │
│ Analytics           │  │ Metrics             │  │ Query Logs   │
│                     │  │                     │  │              │
│ • Requests/sec      │  │ • CPU usage         │  │ • Slow queries│
│ • Bandwidth         │  │ • Memory usage      │  │ • Query count │
│ • Cache hit ratio   │  │ • Request latency   │  │ • Errors      │
│ • Status codes      │  │ • Error rate        │  └──────────────┘
│ • Visitor countries │  │ • Deployment status │
└─────────────────────┘  └─────────────────────┘

┌─────────────────────┐  ┌─────────────────────┐  ┌──────────────┐
│ Cloudflare          │  │ UptimeRobot         │  │ Sentry.io    │
│ Security Events     │  │ Uptime Monitoring   │  │ Error Track  │
│                     │  │                     │  │              │
│ • Blocked requests  │  │ • Uptime %          │  │ • JS errors   │
│ • WAF triggers      │  │ • Response time     │  │ • Stack trace │
│ • Rate limit hits   │  │ • Downtime alerts   │  │ • User impact │
│ • Bot detections    │  │ • Status page       │  │ • Breadcrumbs │
└─────────────────────┘  └─────────────────────┘  └──────────────┘

                      ┌─────────────────────┐
                      │ Custom Logging      │
                      │ (Backend)           │
                      │                     │
                      │ • Winston/Pino      │
                      │ • Structured logs   │
                      │ • Error tracking    │
                      │ • Audit trail       │
                      └─────────────────────┘
```

---

## 💰 Cost Breakdown Visualization

```
FREE TIER (Development)
┌────────────────────────────────────────────┐
│ Cloudflare Free          $0/month          │
│ ├─ Unlimited bandwidth                     │
│ ├─ Basic DDoS protection                   │
│ ├─ Free SSL certificates                   │
│ └─ Basic WAF rules                         │
├────────────────────────────────────────────┤
│ Render Free (Frontend)   $0/month          │
│ ├─ 750 hours/month                         │
│ ├─ Spins down after 15min                  │
│ ├─ 512MB RAM, Shared CPU                   │
│ └─ Free SSL on custom domains              │
├────────────────────────────────────────────┤
│ Render Free (Backend)    $0/month          │
│ └─ Same as frontend                        │
├────────────────────────────────────────────┤
│ TOTAL:                   $0/month          │
└────────────────────────────────────────────┘

PRODUCTION TIER (Recommended)
┌────────────────────────────────────────────┐
│ Cloudflare Pro           $20/month         │
│ ├─ Advanced DDoS protection                │
│ ├─ Enhanced WAF                            │
│ ├─ Image optimization                      │
│ ├─ Web Analytics                           │
│ └─ Priority support                        │
├────────────────────────────────────────────┤
│ Render Starter (Frontend) $7/month         │
│ ├─ Always on (no spin down)                │
│ ├─ 1GB RAM, Shared CPU                     │
│ └─ Custom domain SSL                       │
├────────────────────────────────────────────┤
│ Render Starter (Backend)  $7/month         │
│ └─ Same as frontend                        │
├────────────────────────────────────────────┤
│ Render PostgreSQL         $7/month         │
│ ├─ 1GB database                            │
│ ├─ Automatic backups                       │
│ └─ High availability                       │
├────────────────────────────────────────────┤
│ TOTAL:                   $41/month         │
└────────────────────────────────────────────┘

ENTERPRISE TIER (Scale)
┌────────────────────────────────────────────┐
│ Cloudflare Business     $200/month         │
│ Render Pro (Frontend)    $25/month         │
│ Render Pro (Backend)     $25/month         │
│ Render PostgreSQL Pro    $50/month         │
├────────────────────────────────────────────┤
│ TOTAL:                  $300/month         │
└────────────────────────────────────────────┘
```

---

## 🎯 Traffic Flow at Scale

```
                     ┌──────────────┐
                     │ 10,000 users │
                     │   per day    │
                     └──────┬───────┘
                            │
              ┌─────────────┼─────────────┐
              │                           │
      ┌───────▼────────┐        ┌────────▼───────┐
      │ Cached          │        │ Dynamic        │
      │ Requests (80%)  │        │ Requests (20%) │
      │ 8,000/day       │        │ 2,000/day      │
      └───────┬─────────┘        └────────┬───────┘
              │                           │
              │ Served by                 │ Forwarded to
              │ Cloudflare Edge           │ Render servers
              │ (Instant)                 │
              │                           │
              │                  ┌────────▼───────┐
              │                  │ Load Balancing │
              │                  │ (Render)       │
              │                  └────────┬───────┘
              │                           │
              │                  ┌────────▼───────┐
              │                  │ App Instances  │
              │                  │ • Instance 1   │
              │                  │ • Instance 2   │
              │                  │ • Auto-scale   │
              │                  └────────┬───────┘
              │                           │
              │                  ┌────────▼───────┐
              │                  │ Database Pool  │
              │                  │ Connections    │
              └──────────────────┴────────────────┘

Total Response Time:
• Cached: 50-100ms (Cloudflare edge)
• Dynamic: 200-500ms (Render + DB)
```

---

This architecture provides:
✅ Global CDN performance
✅ Enterprise-grade security
✅ Auto-scaling capabilities
✅ High availability (99.9% uptime)
✅ Cost-effective at any scale
✅ Easy deployment & management
