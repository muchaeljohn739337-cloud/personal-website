# ⚠️ PRODUCTION SECURITY CHECKLIST:
# 1. Generate new NEXTAUTH_SECRET_BASE64 (openssl rand -base64 64)
# 2. Set real SENTRY_AUTH_TOKEN from https://sentry.io/settings/account/api/auth-tokens/
# 3. Generate strong NEXT_PUBLIC_ADMIN_KEY (crypto.randomBytes(32).toString('hex'))
# 4. Set real SMSPOOL_API_KEY from https://www.smspool.net/
# 5. Configure VAPID keys for web push notifications
# 6. Update NEXTAUTH_URL to production domain
# 7. Ensure all API keys are kept secret and not committed to version control

SENTRY_SUPPRESS_GLOBAL_ERROR_HANDLER_FILE_WARNING=1
SENTRY_SOURCEMAPS_DELETE_AFTER_UPLOAD=true

# Sentry Configuration
# ⚠️ REQUIRED: Get token from https://sentry.io/settings/account/api/auth-tokens/
SENTRY_AUTH_TOKEN=your_generated_token_here
SENTRY_ORG=advancia
SENTRY_PROJECT=pay-ledger
SENTRY_TELEMETRY=false

# Production API URL (Backend)
# Use the custom API domain in production
NEXT_PUBLIC_API_URL=https://api.advanciapayledger.com
NEXT_PUBLIC_API_BASE_URL=https://api.advanciapayledger.com

# NextAuth Production Configuration
# ⚠️ REQUIRED: Generate new secret for production!
NEXTAUTH_SECRET_BASE64=GENERATE_NEW_SECRET_HERE
NEXTAUTH_URL=https://advanciapayledger.com

# Admin Dashboard Access
# ⚠️ REQUIRED: Generate strong random key!
NEXT_PUBLIC_ADMIN_KEY=GENERATE_STRONG_KEY_HERE

# SMS Pool API
# ⚠️ REQUIRED: Get from https://www.smspool.net/
SMSPOOL_API_KEY=your_real_api_key_here
